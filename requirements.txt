# Pyron - Autonomous AI Agent with Hierarchical Memory
# No external dependencies required for core functionality
# Uses Python standard library for all memory operations
# Optional: numpy (pip install numpy) for faster vector operations
